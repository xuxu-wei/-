# 制作样章：练习核验与复习

> 历史记录：本页描述 M1 当时的版本。样章已在 M3 正式验收后清理，旧命令与数值仅供追溯；历史文件链接固定在清理前提交，当前使用入口见 [README](../README.md)。

本页归档 M1 的题目答案与核验依据。六节共 22 题：14 道选择题、8 道 Python 计算题。题面、函数参数、样例和约束在 Notebook 与[交互练习页](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json)同步展示。

公开题面为 `exercises/samples/accumulation-clearance/questions.json`，核验材料为 `verification.json`，参考代码为 `solutions.json`。选择题当前版本为 1，Python 题已升级为具名参数的版本 2；旧记录保留，新版重新判定。

## 存量与流量的单位

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：A。

- A：流率乘时间得到区间内实际转移的量，与存量同为 U。
- B：存量是某时刻的物质量，不能把它写成每单位时间的量。
- C：流量带有时间分母，不能与存量直接相加。
- D：乘以时间后，流量单位中的 T 被约掉。

## 计算逐段收支

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · Python 计算题

函数签名：

```python
def solve(
    A0: float,
    durations: list[float],
    inflows: list[float],
    outflows: list[float],
) -> list[float]:
    ...
```

每段使用自己的持续时间；先将初值加入列表，再逐段累积，最后以总流入减总流出核验。

输入输出示例的参考结果：

```python
expected_output = [2, 2.25, 2.375, 2.375]
```

起点为 2 U。第一个区间净增加 0.25×(2−1)=0.25 U；第二个净增加 0.25×(2−1.5)=0.125 U；第三个净变化为零，所以依次得到 2、2.25、2.375、2.375 U。

完整判定包含 3 组输入；返回类型和顺序遵循题面，数值采用题面声明的容差。

## 把收支模型迁移到储水池

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：A、B、D。

- A：存量与流量保留相差一个时间单位的关系。
- B：这是在明确所有跨边界过程后的收支关系。
- C：守恒关系相同并不意味着流出机制相同。
- D：简化是模型的明确假设，而不是被无意忽略的机制。

## 清除系数和每步比例

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：A。

- A：kh 是本步比例，分别为 0.5×0.1 和 0.5×1。
- B：k 不等于任何步长下都不变的每步比例。
- C：应乘以步长，不能除以步长。
- D：(1/T)×T 无量纲。

## 写出逐步更新循环

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · Python 计算题

函数签名：

```python
def solve(
    u: float,
    k: float,
    A0: float,
    h: float,
    steps: int,
) -> list[float]:
    ...
```

每步清除量由该步起点存量计算；默认前三步为 0.1、0.195、0.28525 U。补充用例还检查零清除与负值情形。

输入输出示例的参考结果：

```python
expected_output = [0, 0.1, 0.195, 0.28525]
```

从 0 U 开始：第一步 0+0.1×(1−0.5×0)=0.1；第二步得到 0.195；第三步得到 0.28525。返回列表的第一项仍是起点 0。

完整判定包含 3 组输入；返回类型和顺序遵循题面，数值采用题面声明的容差。

## 恒定流出假设的适用边界

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：A。

- A：流出不能在耗尽后无条件持续；需限制适用时间或明确新的耗尽机制。
- B：裁剪引入了额外规则，必须明确说明并重新核验。
- C：物理合理性不能由程序是否报错决定。
- D：失效的是当前流出假设的适用范围，而不是收支原理。

## 平衡时是否还有物质交换

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：A。

- A：u=kA，抵消的是净变化，不是两个流动过程。
- B：流动可以持续且相互抵消。
- C：本例平衡为 u/k=2 U。
- D：交换存在并不要求净流量为正。

## 在同一平衡位置比较靠近速度

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · Python 计算题

函数签名：

```python
def solve(
    target: float,
    ks: list[float],
    h: float,
    fraction: float,
) -> tuple[list[float], list[float]]:
    ...
```

固定 target 要同步改变 u；先更新，再判断是否达到阈值。用更新步数乘 h 得到首次到达时间。

输入输出示例的参考结果：

```python
expected_output = ([0.5, 1.0],
                   [2.8000000000000003, 1.4000000000000001])
```

target=2 时，两种清除系数所需流入分别为 0.5 和 1 U/T。目标是首次达到 1 U；离散更新分别需要 28 步和 14 步，时刻为 2.8 T 和 1.4 T。

完整判定包含 3 组输入；返回类型和顺序遵循题面，数值采用题面声明的容差。

## 零清除与改变流出机制

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：A、B、D。

- A：此时 A′=u>0，存量持续增长。
- B：没有净变化，每个初值都维持原值。
- C：流出机制变了，平衡方程必须重新求解。
- D：存量增加会加强清除，从而抑制进一步积累。

## 变化率、累积量与初值

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：A。

- A：导数是单位时间变化率，积分累积流量，初值确定本次演化的起点。
- B：求导增加时间分母，积分消去流率中的时间分母。
- C：同一方程可对应不同初值和不同轨迹。
- D：流率可以随时间变化，倍增时间不保证倍增积分。

## 实现含零清除分支的解析解

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · Python 计算题

函数签名：

```python
def solve(
    u: float,
    k: float,
    A0: float,
    times: list[float],
) -> list[float]:
    ...
```

解析式同时满足初值（initial condition）与方程；k=0 时解为 A0+ut，不能让含 u/k 的分支参与计算。

输入输出示例的参考结果：

```python
expected_output = [0.0,
                   0.7869386805747333,
                   1.8358300027522023]
```

本组 A(t)=2·(1−exp(−0.5t))。t=0 时为 0 U；t=1 和 5 时分别约为 0.7869387 U 与 1.8358300 U，按给定时刻顺序返回。

完整判定包含 4 组输入；返回类型和顺序遵循题面，数值采用题面声明的容差。

## 初值加倍的适用条件

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：A。

- A：无输入只有 A0·exp(−kt)；有输入还包含不随 A0 加倍的输入贡献。
- B：这种比例关系有 u=0 等条件，不能无条件推广。
- C：题目没有改变输入，不能把它贡献的物质量也加倍。
- D：初值影响通过衰减项持续存在，只是随时间逐渐减小。

## 怎样核验一个连续模型的解

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：B。

- A：变化率关系不能代替起始条件；同一方程可以有不同起点的轨迹。
- B：正确。核验既要检查变化率关系，也要代入 t=0 检查给定初值。
- C：平滑只描述曲线外观，不保证满足方程和起始条件。
- D：终点接近不能确定整条轨迹，更不能消除起点错误。

## 稳定、非负与准确是不同检查

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：A、B、C。

- A：外观不能替代与独立参考的误差比较。
- B：偏差乘子为 1−kh，绝对值小于 1 时偏差缩小。
- C：取 u=0、A0>0，第一步即可为负。
- D：稳定性控制偏差放大，不给出足够的误差精度保证。

## 保留并识别数值负值

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · Python 计算题

函数签名：

```python
def solve(
    u: float,
    k: float,
    A0: float,
    hs: list[float],
    steps: int,
) -> tuple[list[float], list[float]]:
    ...
```

每组重新设置初值，用该步长的更新式推进。默认三组第一步分别为 −5、2.5、6.25 U；稳定性与非负性要分别判断。

输入输出示例的参考结果：

```python
expected_output = ([-5.0, 2.5, 6.25],
                   [-5.0, 0.009765625, 0.95367431640625])
```

u=0、k=0.75 时，每一步把 A 乘以 1−0.75h。三个步长的第一步为 −5、2.5、6.25 U；前 5 步连同起点的最小值分别为 −5、0.009765625、0.95367431640625 U。

完整判定包含 3 组输入；返回类型和顺序遵循题面，数值采用题面声明的容差。

## 找到满足误差要求的第一个步长

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · Python 计算题

函数签名：

```python
def solve(
    u: float,
    k: float,
    A0: float,
    end: float,
    start_h: float,
    tol: float,
) -> tuple[float, int, float]:
    ...
```

每次减半都重新从同一初值（initial condition）模拟，并独立计算解析参考。返回首次满足严格误差阈值的项，而不是任意更小步长。

输入输出示例的参考结果：

```python
expected_output = (0.03125, 640, 0.005785833855069811)
```

从 0.5 T 依次减半到 0.03125 T 时，首次使最大误差小于 0.01 U。此时 20/0.03125=640 步，网格上的最大误差约为 0.005785834 U。

完整判定包含 3 组输入；返回类型和顺序遵循题面，数值采用题面声明的容差。

## 缩小步长能修复错误机制吗

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：B。

- A：缩小步长只能减少给定离散规则的近似误差，不能修复错误机制。
- B：正确。必须先对照收支和方程确认实现，再用独立依据检查收敛。
- C：平滑不能证实流出机制实现正确。
- D：非负只是物理合理性的一项检查，不能替代机制核对。

## 临界步长的振荡为何不消失

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：A、C、D。

- A：正确。1−kh=−1，每一步只反转符号，绝对值不变。
- B：有界不等于准确；连续模型应逐渐衰减到零，这里的振荡并未衰减。
- C：正确。该例的负物质量暴露了步长导致的失真，不应直接裁剪掩盖它。
- D：正确。模型的性质与数值方法在特定步长下的性质要分别检查。

## 输入突变和存量连续

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：A。

- A：有限流率在无限短时间内累积的物质量趋于零，但两侧变化率可不同。
- B：流率和存量是不同的量，不能同步按倍数跳变。
- C：瞬时存量跳变需要明确的脉冲输入等额外假设。
- D：流率差单位为 U/T，不能直接加到 U 上。

## 计算分段输入的解析终点

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · Python 计算题

函数签名：

```python
def solve(
    bounds: list[float],
    rates: list[float],
    k: float,
    A0: float,
) -> tuple[float, float]:
    ...
```

总输入是各段“流率×时长”的和；终点还取决于各段的清除时间。后置输入默认解析终点约为 3.973048 U。

输入输出示例的参考结果：

```python
expected_output = (20.0, 3.973048212003658)
```

前 10 T 没有流入，物质量保持为零；后 10 T 以 2 U/T 流入，总输入为 20 U。清除持续发生，所以终点为 4·(1−exp(−5))≈3.973048212 U。

完整判定包含 4 组输入；返回类型和顺序遵循题面，数值采用题面声明的容差。

## 在输入变化点截断步长

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · Python 计算题

函数签名：

```python
def solve(
    bounds: list[float],
    rates: list[float],
    k: float,
    A0: float,
    h: float,
) -> tuple[list[float], list[float], list[float], float]:
    ...
```

两条轨迹分别维护初值；数值更新用实际相邻时刻差。检查变化点在网格中、无重复点，并报告同一时刻的最大差。

输入输出示例的参考结果：

```python
expected_output = ([0, 1, 2],
                   [0, 1.0, 0.5],
                   [0,
                    0.7869386805747333,
                    0.4773024370823823],
                   0.21306131942526674)
```

在 [0,1] 内流入为 1，第一步得到 1 U；在 [1,2] 内流入变为 0，第二步得到 0.5 U。网格为 [0,1,2]，解析值约为 [0,0.7869387,0.4773024]，最大差出现在 t=1。

完整判定包含 4 组输入；返回类型和顺序遵循题面，数值采用题面声明的容差。

## 相同总输入是否意味着相同终点

[历史题库](https://github.com/xuxu-wei/dive-2-systems-science/blob/46e5619/exercises/samples/accumulation-clearance/questions.json) · 选择题

正确选项：B。

- A：终点还取决于物质进入后的清除时间，不能只比较累计输入。
- B：正确。晚期进入的物质清除时间较短；仍需把具体输入时序代入模型计算。
- C：早期物质经历更长的清除，不支持这个一般结论。
- D：u(t) 的时间安排是模型输入的一部分；比较方案时必须保留。
